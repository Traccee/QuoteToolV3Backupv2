{
  "name": "quote-tool",
  "version": "1.0.0",
  "description": "Quote Tool App",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  }
}
